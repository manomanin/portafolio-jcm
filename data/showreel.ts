/**
 * Cuando tengas un showreel real: poné el archivo en /public/showreel.mp4
 * (y un poster en /public/showreel-poster.jpg) y cambiá los src acá.
 */
export const showreel = {
  src: null as string | null,
  poster: null as string | null,
  placeholder: true,
};
